# Remo's NixOS Configuration

## Setup

Put the flake under home directory as indicated in `lib/vars/default.nix`

Or change the `flakePath` in that file.


## Things that are done not declaratively

- flatpaks